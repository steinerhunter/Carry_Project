<%= form_for(@payment ||= Payment.new, :url => pay_bill_url, :html => {:id => :payForm}) do |f| %>
    <%= f.label :amount, "Enter amount:" %>
    <%= f.text_field :amount %>
    <%=  f.submit "Pay!" %>
<% end  %>