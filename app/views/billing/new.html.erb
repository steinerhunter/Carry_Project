<%= form_for(:payment, url: pay_bill_path, :html => { :id => "payForm" } ) do |f|  %>
    <%= f.label :amount, "Enter amount:" %>
    <%= f.text_field :amount %>
    <%=  f.submit "Pay!" %>
<% end  %>